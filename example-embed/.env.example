# 复制本文件为 .env 并填入真实密钥（.env 已被 .gitignore 排除，不会提交）
# 运行方式（example-embed 为独立工程）：
#   cd example-embed
#   cp .env.example .env   # 填入 DEFAULT_API_KEY
#   mvn exec:java

# 默认模型
DEFAULT_MODEL=deepseek-chat
DEFAULT_BASE_URL=https://api.deepseek.com
DEFAULT_API_KEY=
