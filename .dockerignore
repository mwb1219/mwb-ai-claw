# ============================================================
# .dockerignore（仓库根目录）：example-web 镜像构建上下文过滤
# 构建上下文 = 仓库根目录（docker compose build context: ..）
# 排除项：版本控制 / IDE / 构建产物 / 密钥 / 文档，减小上下文体积
# ============================================================

# 版本控制
.git
.gitignore

# IDE / 本地工具
.idea
.vscode
*.iml
.DS_Store

# 构建产物
**/target/
**/node_modules/
**/dist/

# 密钥与环境变量（运行时经 docker compose env_file 注入，不进入镜像）
.env
**/.env

# 日志与运行数据
**/*.log
logs/

# 文档 / 截图（镜像不需要）
**/*.md
docs/
screenshots/
