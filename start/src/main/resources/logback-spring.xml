<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!--定义参数,后面可以通过${APP_NAME}使用-->
    <property name="APP_NAME" value="start" />
    <!-- 日志目录支持外部覆盖（CLAW_LOG_PATH），默认 ${user.home}/${APP_NAME}/logs -->
    <property name="LOG_PATH" value="${CLAW_LOG_PATH:-${user.home}/${APP_NAME}/logs}" />
    <property name="LOG_FILE" value="${LOG_PATH}/application.log" />

    <appender name="APPLICATION"
        class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--定义日志输出的路径-->
        <file>${LOG_FILE}</file>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <maxFileSize>50MB</maxFileSize>
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>utf8</charset>
        </encoder>
    </appender>

    <!-- 结构化 JSON 日志（Profile=json，如 spring.profiles.active=web,json）：
         单行 JSON 输出，含时间戳/级别/logger/message/MDC（sessionId/agentId/traceId 等） -->
    <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"app":"start"}</customFields>
            <includeMdc>true</includeMdc>
        </encoder>
    </appender>

    <!--rootLogger：默认 INFO；shell 交互模式下仅输出 ERROR（避免 warn/info 日志干扰终端，
        分层记忆等业务日志已提升为 WARN，在 shell 模式一并被抑制，web 模式仍可见）；
        json 模式输出单行 JSON（与 shell 互斥，优先于默认文本输出）-->
    <springProfile name="json">
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON" />
            <appender-ref ref="APPLICATION" />
        </root>
    </springProfile>
    <springProfile name="shell">
        <root level="ERROR">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="APPLICATION" />
        </root>
    </springProfile>
    <springProfile name="!json &amp; !shell">
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="APPLICATION" />
        </root>
    </springProfile>

    <!--应用日志：继承 root 级别（shell 模式下 root=ERROR 不打印 warn/info；如需调试可临时加
        <logger name="com.mwb.ai.claw" level="DEBUG"/>）-->

    <!--数据库日志-->
    <!--由于这个logger自动继承了root的appender，root中已经有stdout的appender了，自己这边又引入了stdout的appender-->
    <!--如果没有设置 additivity="false" ,就会导致一条日志在控制台输出两次的情况-->
    <!--additivity表示要不要使用rootLogger配置的appender进行输出-->
    <logger name="com.apache.ibatis" level="TRACE" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>
</configuration>
