<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.mwb.ai.claw</groupId>
        <artifactId>mwb-ai-claw-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>mwb-ai-claw-infrastructure</artifactId>
    <packaging>jar</packaging>
    <name>mwb-ai-claw-infrastructure</name>

    <dependencies>
        <dependency>
            <groupId>com.mwb.ai.claw</groupId>
            <artifactId>mwb-ai-claw-domain</artifactId>
        </dependency>
        <!-- client 数据模型与错误码（AgentErrorCode / BizException / ChatCmd 等） -->
        <dependency>
            <groupId>com.mwb.ai.claw</groupId>
            <artifactId>mwb-ai-claw-client</artifactId>
        </dependency>

        <!-- LLM 调用所需：Spring、RestTemplate、Jackson、SLF4J -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!-- 可观测性指标（版本由 spring-boot-dependencies BOM 管理；无 actuator 时用 SimpleMeterRegistry 内存计数） -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- JUnit 4 测试运行所需（Spring Boot 2.4+ 已从 starter-test 移除 vintage engine） -->
        <dependency>
            <groupId>org.junit.vintage</groupId>
            <artifactId>junit-vintage-engine</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
